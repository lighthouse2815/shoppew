name: Security

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "17 3 * * 1"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: security-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  dependency-review:
    name: Dependency review
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Reject newly introduced vulnerable dependencies
        uses: actions/dependency-review-action@v5
        with:
          fail-on-severity: high
          fail-on-scopes: runtime, development, unknown
          license-check: true
          vulnerability-check: true

  codeql:
    name: CodeQL / ${{ matrix.language }}
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    permissions:
      actions: read
      contents: read
      packages: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: java-kotlin
            build-mode: manual
          - language: javascript-typescript
            build-mode: none
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Java 21
        if: matrix.language == 'java-kotlin'
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
          cache-dependency-path: backend/pom.xml

      - name: Set up Android SDK
        if: matrix.language == 'java-kotlin'
        uses: android-actions/setup-android@v4
        with:
          packages: platform-tools

      - name: Install Android compile SDK 36
        if: matrix.language == 'java-kotlin'
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle cache
        if: matrix.language == 'java-kotlin'
        uses: gradle/actions/setup-gradle@v6

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}
          queries: security-extended

      - name: Build Java and Kotlin for CodeQL
        if: matrix.language == 'java-kotlin'
        run: |
          chmod +x backend/mvnw mobile/android/gradlew
          backend/mvnw -B -ntp -f backend/pom.xml -DskipTests package
          mobile/android/gradlew --no-daemon --stacktrace -p mobile/android assembleDebug

      - name: Analyze with CodeQL
        uses: github/codeql-action/analyze@v4
        with:
          category: /language:${{ matrix.language }}

  trivy:
    name: Trivy / source, secrets, four runtime images, SBOMs
    runs-on: ubuntu-24.04
    timeout-minutes: 50
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Scan source dependencies, secrets, and configuration
        env:
          TRIVY_OFFLINE_SCAN: "true"
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          scanners: vuln,misconfig,secret
          format: table
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: 1

      - name: Build immutable backend image
        run: docker build --tag shoppew-backend:${{ github.sha }} backend

      - name: Build immutable Storefront image
        run: |
          docker build \
            --file deploy/docker/Dockerfile.storefront \
            --build-arg NEXT_PUBLIC_API_URL=https://api.shoppew.invalid \
            --build-arg NEXT_PUBLIC_MEDIA_ORIGIN=https://media.shoppew.invalid \
            --build-arg NEXT_PUBLIC_SITE_URL=https://shoppew.invalid \
            --tag shoppew-storefront:${{ github.sha }} \
            .

      - name: Build immutable Seller Center image
        run: |
          docker build \
            --file deploy/docker/Dockerfile.seller \
            --build-arg VITE_API_URL=https://api.shoppew.invalid \
            --tag shoppew-seller:${{ github.sha }} \
            .

      - name: Build immutable Admin image
        run: |
          docker build \
            --file deploy/docker/Dockerfile.admin \
            --build-arg VITE_API_URL=https://api.shoppew.invalid \
            --build-arg VITE_STOREFRONT_URL=https://shoppew.invalid \
            --tag shoppew-admin:${{ github.sha }} \
            .

      - name: Scan backend runtime image
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: shoppew-backend:${{ github.sha }}
          format: table
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: 1
          skip-setup-trivy: true

      - name: Scan Storefront runtime image
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: shoppew-storefront:${{ github.sha }}
          format: table
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: 1
          skip-setup-trivy: true

      - name: Scan Seller Center runtime image
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: shoppew-seller:${{ github.sha }}
          format: table
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: 1
          skip-setup-trivy: true

      - name: Scan Admin runtime image
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: shoppew-admin:${{ github.sha }}
          format: table
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: 1
          skip-setup-trivy: true

      - name: Generate backend CycloneDX image SBOM
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: shoppew-backend:${{ github.sha }}
          format: cyclonedx
          output: shoppew-backend-sbom.cdx.json
          skip-setup-trivy: true

      - name: Generate Storefront CycloneDX image SBOM
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: shoppew-storefront:${{ github.sha }}
          format: cyclonedx
          output: shoppew-storefront-sbom.cdx.json
          skip-setup-trivy: true

      - name: Generate Seller Center CycloneDX image SBOM
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: shoppew-seller:${{ github.sha }}
          format: cyclonedx
          output: shoppew-seller-sbom.cdx.json
          skip-setup-trivy: true

      - name: Generate Admin CycloneDX image SBOM
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: shoppew-admin:${{ github.sha }}
          format: cyclonedx
          output: shoppew-admin-sbom.cdx.json
          skip-setup-trivy: true

      - name: Retain runtime SBOMs
        uses: actions/upload-artifact@v7
        with:
          name: shoppew-runtime-sboms-${{ github.sha }}
          path: shoppew-*-sbom.cdx.json
          if-no-files-found: error
          retention-days: 30
