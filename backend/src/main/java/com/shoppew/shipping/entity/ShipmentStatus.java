package com.shoppew.shipping.entity;

public enum ShipmentStatus {
    PENDING,
    READY,
    PICKED_UP,
    IN_TRANSIT,
    OUT_FOR_DELIVERY,
    DELIVERED,
    FAILED,
    RETURNING,
    RETURNED
}
