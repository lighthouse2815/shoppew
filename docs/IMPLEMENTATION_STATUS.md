# shoppew implementation status

Last updated: 2026-08-20

Status meanings:

- `NOT_STARTED`: no implementation evidence yet.
- `IN_PROGRESS`: active work exists but the required vertical slice is incomplete.
- `IMPLEMENTED`: integrated code exists but complete verification evidence is not yet recorded.
- `VERIFIED`: relevant build, automated tests, and runtime checks currently pass.

| Phase | Scope | Status | Evidence / next gate |
| --- | --- | --- | --- |
| 0 | Repository, instructions, repo skills, status tracking | VERIFIED | Empty repository inspected; Git initialized on `main`; `AGENTS.md`, design system, and repo-scoped `shoppew-ui` workflow created; `quick_validate.py` reports `Skill is valid!`. |
| 1 | Architecture, workspace, infrastructure, Docker, database | VERIFIED | Architecture/database docs created; Compose config passes; PostgreSQL, Redis, MinIO, Mailpit, and backend are healthy; MinIO bucket initialized. Final `clean verify` created two fresh PostgreSQL 17.10 Testcontainers databases and validated/applied all 11 Flyway migrations; the isolated final Compose run also started from zero users/products/shops and reported `11|true`. |
| 2 | Backend foundation, Flyway, security, common API | VERIFIED | Java 21 / Spring Boot 4.1 compiles 413 sources and packages a runnable JAR. Final `mvnw clean verify` passes 45/45 tests. Runtime readiness is `UP`; the non-root container runs as UID 10001; public envelopes, correlation IDs, protected JSON 401, and Swagger/OpenAPI bearer inheritance with explicit public/login exemptions are verified on both the main and fresh stacks. |
| 3 | Authentication, users, addresses, shops | VERIFIED | 87-source backend compiles; Testcontainers suite passes 8/8 plus SMTP MIME regression 1/1. Verified registration/login, profile/address ownership, shop/member/settings/addresses, HS512 issuance, rotating HttpOnly refresh cookies, reuse-family revocation, immediate access-session revocation, trusted-Origin rejection, one-time email verification/reset, and password-change session revocation. Rebuilt non-root Docker image is healthy; live HTTP persisted customer/address/shop/settings (`PENDING`, `VND`, `Asia/Ho_Chi_Minh`), Mailpit received reset email, Flyway is current, and backend logs contained zero errors after the corrected smoke. |
| 4 | Categories, brands, products, variants, media | VERIFIED | Upload validator passes 5/5 for valid PNG, MIME/signature spoofing, truncated/invalid images, unsupported formats, dimension/pixel limits, and the 5 MB boundary. Testcontainers covers category hierarchy/cycle rejection, brand and typed required attributes, product/options/variants, duplicate combinations, moderation, and public discovery. The fresh-stack smoke persisted `ACTIVE|1`, stored a real PNG in MinIO, and downloaded its 68 bytes as `image/png` with HTTP 200. |
| 5 | Inventory and multi-seller cart | VERIFIED | Backend now has 185 sources, 23 JPA repositories, Flyway V6 backfill/composite cart integrity, seller inventory adjustment/history APIs, atomic reservation/release/consume, configurable expiry scanning, and grouped multi-seller cart revalidation. Full Maven suite passes 16/16. A 100-attempt Testcontainers stress test with stock=1 records exactly one winner, `available=0`, `reserved=1`, one reservation/RESERVE ledger entry, then expiry restores `available=1`, `reserved=0` with one RELEASE. Two-shop cart integration proves current database price/stock/eligibility and rejects insufficient additions. Rebuilt Docker runtime is healthy and `scripts/smoke-catalog.ps1` verifies real seller stock changes plus customer cart regroup/revalidation (`INSUFFICIENT_STOCK`) with final stock restored to 25. |
| 6 | Checkout, orders, payment | VERIFIED | Backend now has 240 sources and 33 JPA repositories. Checkout revalidates owned cart items, address, active seller/catalog state, current price, stock, shipping method, and payment provider; one checkout splits into one immutable-snapshot order per shop. Explicit customer/seller transitions append order history and shipment tracking. COD and signed `MOCK_ONLINE` callbacks share a provider abstraction; checkout keys and payment events are payload-bound and idempotent. Full Maven suite passes 18/18, including two-shop COD/snapshot/state-machine coverage and signed duplicate/mismatched webhook coverage. Rebuilt Docker is healthy at Flyway V7; `scripts/smoke-catalog.ps1` reports `Status: PASS` for address, authoritative preview (`438000 + 22000 = 460000 VND`), COD placement/replay, seller fulfillment, customer completion, snapshot reads, and PostgreSQL evidence `COMPLETED|PENDING|23|0|2`. |
| 7 | Vouchers, promotions, shipping | VERIFIED | Backend now has 273 sources, 38 JPA repositories, and Flyway V8 checkout-bound voucher/promotion usage ledgers. Seller and admin APIs enforce owner/type/product/variant scope; explicit activation/pause/archive states; fixed/percentage, maximum, minimum-spend, time, quantity, per-user, product/category, payment, and currency rules. Active promotion prices appear consistently with original-price metadata in public catalog, cart, checkout, and immutable order snapshots. Checkout allocates shop/platform/shipping vouchers per seller order, locks voucher rows, atomically reserves promotion capacity, and consumes/releases every usage with payment/cancellation. Full suite passes 21/21. A concurrent two-checkout test with `perUserLimit=1` records exactly one `201`, one `409`, one voucher application, and one sold item. Online payment failure restores inventory, promotion and voucher quotas; expired voucher is rejected. Rebuilt Docker is healthy at V8; `scripts/smoke-catalog.ps1` reports `Status: PASS` with authoritative total `400000 + 22000 - 15000 = 407000 VND` and PostgreSQL discount evidence `2|1|2|2`. Mock shipping quote, method, shipment, and tracking remain verified in the same runtime flow. |
| 8 | Reviews, wishlist, notifications | VERIFIED | Backend now has 301 sources and 43 JPA repositories. Wishlist adds are idempotent under the database user/product uniqueness constraint and enforce user ownership. Reviews require an authenticated customer's immutable order item on a `COMPLETED` order, permit one review per item, validate rating 1–5 and up to five images, expose owned/public views, authorize seller replies by shop membership, and support admin publish/hide/remove moderation while transactionally recomputing product/shop average and count. Every order status event persists an owned in-app notification plus delivery record with list/unread/read/read-all APIs. Full Maven suite passes 22/22, including pre-completion/duplicate review rejection, image upload, aggregate recomputation, moderation, wishlist replay/delete, and five-event notification lifecycle. Rebuilt Docker is healthy at Flyway V8; runtime smoke `20260811031543` reports `Status: PASS`, with PostgreSQL evidence `PUBLISHED|5|1|5.00|1|5.00|5|5|1|1`, Flyway `8|true`, and no recent backend `ERROR/Exception`. Storefront and real-device Android notification/review/wishlist integration is now recorded in Phases 10 and 13; the production push-provider gap remains explicit. |
| 9 | Refunds, disputes, seller finance, audit, analytics | VERIFIED | Backend now has 350 sources, 50 JPA repositories, and Flyway V9 refund/finance snapshot and idempotency hardening. Customer refund requests derive prorated amounts and remaining quantities from owned immutable order items and the configured return window; seller review and admin approve/reject/process commands enforce state, actor scope, notes, and an idempotent processing key. Successful processing persists a refund, updates order/payment partial/full-refund states, and deducts the snapshotted seller charge once. Delivery/completion events post gross, seller discount, configurable platform fee, and seller net through an append-only unique-reference ledger into locked pending/available balances. Disputes persist authorized customer/seller/admin timelines and admin resolutions. Critical category/product/shop/review/refund/dispute operations write actor, before/after where available, IP, user agent, request ID, and timestamp audit records. Seller/admin analytics query real order, ledger, refund, user, shop, moderation, product, and inventory data over bounded ranges. Full Maven suite passes 23/23; the Phase 9 test uses separate buyer/seller/outsider identities, proves outsider denial, `190000 -> 90000` balance movement, one refund under replay, one ledger effect, dispute resolution, three scoped audits, and real analytics. Rebuilt Docker upgraded V8 to V9 and is healthy; smoke `20260811041607` reports `Status: PASS` with operations evidence `REFUNDED|SUCCEEDED|PARTIALLY_REFUNDED|PARTIALLY_REFUNDED|RESOLVED|175500.00|5|3`, Flyway `9|true`, and no backend `ERROR/Exception` in the checked 20-minute window. |
| 10 | Customer storefront | VERIFIED | Next.js 16/React 19 storefront ships 23 App Router routes covering home, search/filter, category, product, shop, auth/recovery, cart, authoritative checkout, success, profile, addresses, orders/detail, wishlist, reviews, notifications, messages, and session security. Shared packages provide repo design tokens, accessible state primitives, a generated OpenAPI schema, and a credentialed API client with in-memory access tokens plus one rotating HttpOnly refresh retry. Current aggregate gates pass: lint, typecheck, Vitest 27/27, and production build with 18 static pages plus dynamic catalog/account routes. Browser QA at 360/768/1280 and the final Playwright lifecycle verify validation, session restore/logout, browse/cart, authoritative preview, COD placement, completion, six-entry history, immutable snapshot, media fallback, chat, and SEO/security metadata behavior against the real backend. |
| 11 | Seller Center | VERIFIED | Vite/React Seller Center covers onboarding, live dashboard, shop settings/addresses, products with attributes/options/variants/media, inventory ledger, order snapshots and fulfillment commands, refunds/disputes, vouchers, multi-target promotions, reviews/replies, finance, analytics, and chat. ESLint and TypeScript pass, Vitest passes 12/12, and the split production build succeeds with a 282.50 kB entry chunk (89.99 kB gzip) plus route chunks. Real-browser QA exercised every route at 360/768/1280. Final Playwright creates a draft through the UI with required attributes, a real PNG, variant and stock, submits it, then processes a buyer order through process/ready/ship/deliver. Backend tests prove seller membership/isolation and the category attribute-definition contract. |
| 12 | Admin application | VERIFIED | The Vite/React Admin exposes 16 canonical protected routes for live analytics, users/sellers/shops, catalog moderation, orders/payments/refunds/disputes, vouchers/promotions, reviews, audit logs, and effective runtime settings. `/settings` is intentionally read-only: it performs `GET /api/v1/admin/settings` and does not claim mutable configuration. Current gates pass: ESLint, TypeScript, Vitest 18/18, and the production build (400.04 kB JS and 34.85 kB CSS before gzip). Real-browser QA exercised all 16 routes without page alerts or console errors, read live dashboard values, searched and inspected users, suspended then restored an older synthetic buyer through protected APIs, confirmed both audit events, inspected an immutable order snapshot/history, and verified the 390 x 844 navigation drawer, Escape close, and focus restoration. Backend role checks remain authoritative; `MODERATOR` is limited to product/review moderation while `ADMIN`/`SUPER_ADMIN` receive the full surface. |
| 13 | Native Android application | VERIFIED | The Kotlin/Compose client uses the real `/api/v1` backend for session restore/login/register, catalog/search/detail/variants, cart, addresses, authoritative checkout/COD order placement, orders/detail, wishlist, notifications, reviews, profile, and security settings. The access token remains in memory; the rotating refresh cookie is encrypted with AES-GCM using an Android Keystore key. Room is a read-only catalog/search-history/recently-viewed fallback and never queues commerce mutations. Earlier gates passed 24/24 JVM tests, `lintDebug`, debug assembly, Android-test compilation, and 7/7 connected tests on the real Samsung `SM-G988B` running Android 13. No emulator was installed or used. Real-device QA over `adb reverse` covered the backend flow, process/session restoration, deep links, offline cached browsing, and the contextual Android 13 notification-permission UI; `POST_NOTIFICATIONS` was confirmed `granted=true`. The newer authenticated FID registration/revocation path is tracked separately in Phase 22 and requires a newly authorized physical-device delivery check before it can inherit that device evidence. |
| 14 | Chat, search, recommendations, polish | VERIFIED | Flyway V10 adds the full-text GIN index and persistent product views; V11 adds `pg_trgm`, a trigram product-name GIN index, and an active-variant price index. `SearchService` supports keyword, shop/category/brand, price, rating, all required sorts, pagination, and suggestions; `RecommendationService` supplies popular, 7-day trending, related/same-category fallback, same-shop, and authenticated recently viewed results without an ML dependency. Customer/shop-member chat persists `TEXT`, `IMAGE`, `PRODUCT`, and `ORDER` messages, scopes every read/send to the authenticated participant, emits owned `CHAT` notifications, and publishes a bearer-authenticated STOMP user queue at `/api/v1/ws`. Focused Testcontainers coverage passes 2/2 and the final clean backend gate includes it in 45/45 passing tests. Live browser QA verified filtered discovery and a two-way persisted buyer/seller conversation; the regenerated TypeScript OpenAPI client typechecks. |
| 15 | End-to-end verification | VERIFIED | `pnpm typecheck:e2e` passes. The final system-Chrome run uses the real backend, one worker, and passes 4/4 tests in 78.8 seconds: buyer browse/cart/authoritative preview/COD/new order; Seller UI product draft with required attributes/PNG/variant/inventory/submit plus process/ready/ship/deliver; buyer completion with six-entry history and immutable snapshot; Admin approval and user/shop/completed-order/audit inspection. Teardown leaves the listing `ARCHIVED`, cart count 0, and zero non-terminal Playwright lifecycle orders. No API commerce mocking or Android emulator is used. |
| 16 | Security review | VERIFIED | Focused backend security regressions pass 21/21 and are included in final `clean verify` 45/45. Review and fixes cover production secret/HTTPS/CORS/mock-provider fail-fast, rotated/revocable sessions and origin/CSRF rules, customer/seller/admin object authorization, payment/voucher idempotency, strict image structure/dimension/pixel limits, URL/log sanitization, authenticated STOMP destinations and principal persistence, and Redis fixed-window limits with a bounded fail-closed local fallback. Storefront credential/navigation regressions pass; production pnpm audit reports no known high vulnerabilities and a high-confidence secret scan has no hits. Remaining external boundaries (provider credentials, production proxy/CSP observation, and full dependency/container SCA) are documented rather than claimed. |
| 17 | Performance review | VERIFIED | Route-level splitting and lazy media are present in Seller/Admin; Storefront uses Next Image, responsive sizes, request caching, and server rendering; Android uses Coil, keyed lazy lists, Room read-only fallback, and stable/saveable Compose state. Backend search is paginated and batch-assembles list summaries; V10/V11 indexes are applied. On a documented 23-product local Docker snapshot, public catalog passed 100/100 at concurrency 10 (96.5 req/s, p95 196.3 ms) and Unicode search `áo` passed 100/100 (287.5 req/s, p95 74.8 ms). The 180-request rate-limit boundary returned expected 429s. `docs/PERFORMANCE.md` records methodology and explicitly does not present this as a production SLO/capacity result. |
| 18 | Documentation and CI | VERIFIED | README covers zero-to-run requirements, environment, Docker, database, all clients, tests/E2E, synthetic demo users, Swagger, and troubleshooting. Architecture, database, API, auth, security, deployment, testing, Android, storefront, seller, discovery/chat, and performance docs are linked. The API-contract fixture originally failed because its 53-byte CI JWT secret violated the backend's 64-byte HS512 guard; local reproduction captured the exact startup exception, commit `b517c36` fixed the fixture, and hosted GitHub Actions run `32328281486` then completed all four jobs successfully: backend, web, Android, and live OpenAPI/TypeScript contract comparison. |
| 19 | Fresh-environment final integration | VERIFIED | Isolated project `shoppew-final-202608111202` started with new PostgreSQL/Redis/MinIO/Mailpit volumes and separate ports. Before smoke it had zero users/products/shops; all services became healthy, readiness was `UP`, and Flyway reported 11/11. Smoke run `20260811120258` passed real auth/catalog/MinIO/cart/checkout/COD/order/voucher/promotion/review/notification/refund/dispute/finance/audit flows. Post-checks: PNG HTTP 200 (`image/png`, 68 bytes), Mailpit 4 messages, OpenAPI bearer matrix correct, backend UID 10001, and no strict ERROR/Exception log lines. Final aggregate gates pass: backend 45/45 and JAR, web lint/typecheck/54 tests/three production builds, E2E 4/4, Android 24 JVM + 7 real-device tests/lint/assemble. The isolated stack and its four disposable volumes were then removed; the main five-service stack remained healthy. |
| 20 | Production commerce capability boundary | VERIFIED | Production fail-fast now requires enabled email delivery and rejects both mock payment and mock shipping. `GET /api/v1/public/commerce-capabilities` exposes only the provider methods actually registered by the backend. Storefront checkout loads that contract, renders only available payment/shipping choices, retries capability and preview errors, blocks placement when no provider exists, and never silently falls back to a mock; Android no longer exposes the local online-payment mock. Current evidence: production-validator tests 3/3, fresh-Testcontainers public capability test 1/1 with Flyway V1-V11, live readiness plus runtime capability response, generated OpenAPI equality, Storefront lint/typecheck/Vitest 27/27/production build, and Android `CommerceViewModelsTest` build/test success. Real carrier/payment/SMTP credentials and deployed delivery remain external release gates, not claimed by this phase. |
| 21 | Production web artifacts and security automation | IMPLEMENTED | Repository-owned Dockerfiles build Next.js Storefront standalone plus Seller/Admin static SPA images. Production builds require explicit HTTPS origins; all four runtime images use non-root users and the web images add health checks, SPA fallback where applicable, and CSP/frame/MIME/opener/referrer/permissions headers. Local image/runtime evidence passes for the three web images and backend: HTTP 200/fallback/header assertions, backend readiness `UP`, Flyway V12, and effective UIDs `1000/101/101/10001`. Trivy reports zero fixed High/Critical findings for the rebuilt backend, Storefront, Seller, and Admin images after upgrading the Alpine runtime packages, removing unused global npm tooling, and pinning patched JDBC/Netty/HttpCore versions. The security workflow now adds dependency review, dual-language CodeQL, source/config/secret scanning, all four runtime-image scans, four CycloneDX SBOMs, and six-ecosystem Dependabot coverage. Mark this phase `VERIFIED` only after the exact commit passes both hosted workflows. |
| 22 | Android push lifecycle and durable delivery retry | IMPLEMENTED | Authenticated FID registration/revocation, ownership transfer, per-user active-device bounds, SHA-256 lookup plus AES-256-GCM target storage, conditional Firebase Admin delivery, unregistered-target deactivation, and PostgreSQL-backed bounded retry are integrated. Android resolves the current stable FID after refresh/login/registration and revokes the cached FID before logout; lower-level FCM token rotation does not alter this server target. Current aggregate evidence passes: backend `clean verify` 48/48 with Flyway V1-V12 on fresh PostgreSQL 17.10, API ownership/encryption and codec tamper tests, delivery retry/final-success tests, live image readiness and non-root check, generated OpenAPI equality plus API-client typecheck, and Android 25/25 JVM tests with lint, debug APK assembly, and AndroidTest compilation. Hosted CI is still required before marking `VERIFIED`; Firebase credentials/client configuration and a newly authorized physical-device delivery remain external release gates rather than repository defects. |

## Acceptance accounting

The master prompt's final acceptance checklist remains authoritative. No item is considered complete merely because a file, endpoint, or screen exists. Update this file only with current evidence and preserve failures or skipped checks explicitly.
