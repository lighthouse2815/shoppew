"use client";

import Link from "next/link";
import { useRouter } from "next/navigation";
import { useMutation, useQuery } from "@tanstack/react-query";
import { useMemo, useState } from "react";
import { Button, ErrorState, Field, Price, Spinner } from "@shoppew/ui";
import { CreditCard, MapPin, ShieldCheck, Truck } from "lucide-react";
import { RequireAuth } from "@/components/require-auth";
import { useAuth } from "@/components/providers";
import type { Address, Cart, CheckoutPreview, CheckoutResult } from "@/lib/types";

function CheckoutContent() {
  const { request } = useAuth(); const router = useRouter(); const [addressId, setAddressId] = useState(""); const [provider, setProvider] = useState<"COD" | "MOCK_ONLINE">("COD"); const [voucher, setVoucher] = useState(""); const [note, setNote] = useState("");
  const cart = useQuery({ queryKey: ["cart"], queryFn: () => request<Cart>("/api/v1/cart") }); const addresses = useQuery({ queryKey: ["addresses"], queryFn: () => request<Address[]>("/api/v1/users/me/addresses") });
  const selectedIds = useMemo(() => cart.data?.shops?.flatMap((shop) => shop.items?.filter((item) => item.selected && item.eligible).flatMap((item) => item.id ? [item.id] : []) ?? []) ?? [], [cart.data]);
  const selectedAddress = addressId || addresses.data?.find((item) => item.defaultAddress)?.id || addresses.data?.[0]?.id || "";
  const body = useMemo(() => ({ cartItemIds: selectedIds, addressId: selectedAddress, paymentProvider: provider, customerNote: note || undefined, voucherCodes: voucher.trim() ? voucher.split(",").map((code) => code.trim()).filter(Boolean) : undefined }), [note, provider, selectedAddress, selectedIds, voucher]);
  const preview = useQuery({ queryKey: ["checkout-preview", body], queryFn: () => request<CheckoutPreview>("/api/v1/checkout/preview", { method: "POST", body }), enabled: selectedIds.length > 0 && Boolean(selectedAddress), retry: false });
  const place = useMutation({ mutationFn: () => request<CheckoutResult>("/api/v1/checkout", { method: "POST", headers: { "Idempotency-Key": crypto.randomUUID() }, body }), onSuccess: (result) => { const query = new URLSearchParams({ checkout: result.checkoutNumber ?? "", total: String(result.grandTotal ?? 0), currency: result.currency ?? "VND", orders: result.orders?.map((order) => order.id).filter(Boolean).join(",") ?? "" }); router.replace(`/order/success?${query}`); } });
  if (cart.isPending || addresses.isPending) return <div className="shell page-section"><Spinner label="Đang chuẩn bị thanh toán" /></div>; if (cart.error || addresses.error) return <div className="shell page-section"><ErrorState message={(cart.error ?? addresses.error)?.message ?? "Không thể chuẩn bị checkout."} onRetry={() => { void cart.refetch(); void addresses.refetch(); }} /></div>;
  if (!selectedIds.length) return <main className="shell narrow-page page-section"><div className="notice notice--error">Không có sản phẩm hợp lệ được chọn. <Link href="/cart">Quay lại giỏ hàng</Link>.</div></main>;
  if (!addresses.data?.length) return <main className="shell narrow-page page-section"><div className="notice notice--error">Bạn cần thêm địa chỉ nhận hàng trước khi thanh toán.</div><Link className="sp-button" href="/account/addresses">Thêm địa chỉ</Link></main>;
  return <main className="shell page-section"><div className="section-heading"><div><span className="eyebrow">Kiểm tra lần cuối</span><h1>Thanh toán</h1><p>Máy chủ sẽ kiểm tra lại giá, tồn kho và voucher trước khi tạo đơn.</p></div></div><div className="checkout-layout"><section className="stack"><article className="surface checkout-block"><h2><MapPin /> Địa chỉ nhận hàng</h2><div className="address-choice">{addresses.data.map((address) => <label key={address.id}><input type="radio" name="address" value={address.id} checked={selectedAddress === address.id} onChange={() => setAddressId(address.id ?? "")} /><span><strong>{address.recipientName} · {address.phone}</strong><small>{[address.addressLine, address.ward, address.district, address.province].filter(Boolean).join(", ")}</small></span>{address.defaultAddress && <em>Mặc định</em>}</label>)}</div></article><article className="surface checkout-block"><h2><CreditCard /> Phương thức thanh toán</h2><label className="payment-choice"><input type="radio" checked={provider === "COD"} onChange={() => setProvider("COD")} /><span><strong>Thanh toán khi nhận hàng</strong><small>Thanh toán trực tiếp khi đơn được giao.</small></span></label><label className="payment-choice"><input type="radio" checked={provider === "MOCK_ONLINE"} onChange={() => setProvider("MOCK_ONLINE")} /><span><strong>Cổng thanh toán mô phỏng</strong><small>Dùng cho kiểm thử luồng local; không phải tiền thật.</small></span></label></article><article className="surface form-grid"><Field label="Mã voucher" hint="Có thể nhập nhiều mã, phân cách bằng dấu phẩy." value={voucher} onChange={(e) => setVoucher(e.target.value)} /><Field label="Ghi chú cho nhà bán" value={note} onChange={(e) => setNote(e.target.value)} /></article></section><aside className="surface checkout-summary"><h2>Tóm tắt đơn</h2>{preview.isFetching && <Spinner label="Đang tính lại tổng" />}{preview.error && <p className="notice notice--error">{preview.error.message}</p>}{preview.data && <><div className="checkout-shops">{preview.data.shops?.map((shop) => <div key={shop.shopId}><strong>{shop.shopName}</strong><span>{shop.cartItemIds?.length ?? 0} sản phẩm</span><span><Truck /> Phí giao: {(shop.shippingFee ?? 0).toLocaleString("vi-VN")} ₫</span></div>)}</div><div className="order-totals"><div><span>Tiền hàng</span><span>{(preview.data.itemsSubtotal ?? 0).toLocaleString("vi-VN")} ₫</span></div><div><span>Phí vận chuyển</span><span>{(preview.data.shippingTotal ?? 0).toLocaleString("vi-VN")} ₫</span></div><div><span>Giảm giá</span><span>-{(preview.data.discountTotal ?? 0).toLocaleString("vi-VN")} ₫</span></div><div className="total"><strong>Tổng thanh toán</strong><Price value={preview.data.grandTotal ?? 0} currency={preview.data.currency} /></div></div></>}<Button disabled={!preview.data || preview.isFetching || place.isPending} onClick={() => place.mutate()}>{place.isPending ? "Đang tạo đơn..." : "Đặt hàng"}</Button><small><ShieldCheck /> Bằng việc đặt hàng, bạn xác nhận thông tin giao nhận ở trên là chính xác.</small>{place.error && <p className="notice notice--error">{place.error.message}</p>}</aside></div></main>;
}

export default function CheckoutPage() { return <RequireAuth><CheckoutContent /></RequireAuth>; }
